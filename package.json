{
  "name": "lyrax",
  "version": "1.0.0",
  "description": "Solo Project - Reboot",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "start": "concurrently \"npm install\" \"cd client && npm install\" \"cd server && npm install\" && concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9"
  },
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
